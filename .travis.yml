sudo: required
services:
  - docker

node_js:
  - "11.10.1"

before_install:
  - docker build -t caseylaw/docker-react -f Dockerfile.dev .

script:
  - docker run caseylaw/docker-react npm run test -- --coverage